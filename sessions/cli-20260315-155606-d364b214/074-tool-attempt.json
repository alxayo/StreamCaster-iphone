{
  "sessionId": "cli-20260315-155606-d364b214",
  "toolName": "task",
  "toolArgs": {
    "agent_type": "general-purpose",
    "description": "EncoderController T-020",
    "mode": "background",
    "prompt": "\nYou are building StreamCaster (`com.port80.app`), an iOS RTMP streaming app at `/Users/alex/Code/rtmp-client/iphone/StreamCaster/`.\n\n**Your Task (T-020):** Implement EncoderController as a Swift actor that serializes all quality changes from ABR and thermal systems.\n\n**IMPORTANT CODE STYLE:** Add beginner-friendly comments. Prefer simple code.\n\n**First**, read existing files:\n```bash\ncat /Users/alex/Code/rtmp-client/iphone/StreamCaster/Services/EncoderBridge.swift\ncat /Users/alex/Code/rtmp-client/iphone/StreamCaster/Data/Models/StreamConfig.swift\ncat /Users/alex/Code/rtmp-client/iphone/StreamCaster/Data/Models/StreamStats.swift\n```\n\n**Requirements:**\n\nCreate `/Users/alex/Code/rtmp-client/iphone/StreamCaster/Services/EncoderController.swift`:\n\n```swift\nimport Foundation\n\n/// EncoderController serializes all encoder quality changes.\n///\n/// WHY IS THIS NEEDED?\n/// Two systems can request encoder changes at the same time:\n/// 1. ABR (Adaptive Bitrate) — adjusts quality based on network speed\n/// 2. Thermal Monitor — reduces quality when the device gets hot\n///\n/// If both try to change the encoder simultaneously, it could crash.\n/// Swift's `actor` model ensures only ONE change happens at a time.\n///\n/// HOW IT WORKS:\n/// - Bitrate-only changes are instant (no encoder restart needed)\n/// - Resolution/FPS changes require a restart (detach camera → reconfigure → reattach)\n/// - Thermal changes have a 60-second cooldown to prevent rapid oscillation\n/// - If a thermal-restored config triggers another thermal event within the\n///   cooldown window, that config is blacklisted for the rest of the session\nactor EncoderController {\n    \n    /// Reference to the encoder bridge (does the actual encoding work)\n    private let encoderBridge: EncoderBridgeProtocol\n    \n    /// The current effective quality settings\n    private(set) var currentResolution: Resolution\n    private(set) var currentFps: Int\n    private(set) var currentBitrateKbps: Int\n    \n    /// Thermal cooldown tracking\n    private var lastThermalRestartTime: Date = .distantPast\n    private let thermalCooldownSeconds: TimeInterval = 60\n    \n    /// Progressive thermal restoration backoff\n    private var thermalRestorationAttempt: Int = 0\n    \n    /// Configs that caused thermal escalation — blacklisted for this session\n    private var blacklistedConfigs: Set<String> = []\n    \n    /// Whether a restart is currently in progress\n    private var isRestarting: Bool = false\n    \n    init(encoderBridge: EncoderBridgeProtocol, initialConfig: StreamConfig) { ... }\n    \n    /// Called by the ABR system when network conditions change.\n    /// Bitrate-only changes are applied immediately.\n    /// Resolution/FPS changes trigger an encoder restart.\n    func requestAbrChange(bitrateKbps: Int, resolution: Resolution? = nil, fps: Int? = nil) async throws { ... }\n    \n    /// Called by the Thermal Monitor when device temperature changes.\n    /// Subject to 60-second cooldown between restart-class changes.\n    func requestThermalChange(resolution: Resolution, fps: Int) async throws { ... }\n    \n    /// Attempt to restore quality after thermal conditions improve.\n    /// Uses progressive backoff: 60s, 120s, 300s between attempts.\n    func requestThermalRestore(resolution: Resolution, fps: Int, bitrateKbps: Int) async throws { ... }\n    \n    // MARK: - Private Helpers\n    \n    /// Execute the encoder restart sequence\n    private func executeRestart(resolution: Resolution, fps: Int, bitrateKbps: Int) async throws {\n        // 1. Mark restart in progress\n        // 2. Detach camera\n        // 3. Update video settings\n        // 4. Reattach camera\n        // 5. Request keyframe (IDR)\n        // 6. Wait for confirmation (up to 5 seconds)\n        // 7. Mark restart complete\n    }\n    \n    /// Generate a config key for blacklisting\n    private func configKey(resolution: Resolution, fps: Int) -> String {\n        \"\\(resolution.width)x\\(resolution.height)@\\(fps)\"\n    }\n}\n```\n\nWire into DependencyContainer.\n\nAfter completing, update SQL:\n```sql\nUPDATE todos SET status = 'done' WHERE id = 'T-020';\n```\n\nThen commit:\n```bash\ncd /Users/alex/Code/rtmp-client/iphone\ngit add -A\ngit commit -m \"feat: implement actor-serialized EncoderController\n\n- Add EncoderController as Swift actor for thread-safe quality changes\n- Serialize concurrent ABR and thermal encoder requests\n- Bitrate-only changes applied immediately (no restart)\n- Resolution/FPS changes trigger controlled encoder restart sequence\n- 60-second cooldown for thermal-triggered restarts\n- Progressive restoration backoff (60s, 120s, 300s)\n- Blacklist configs that cause thermal oscillation\n- 5-second timeout waiting for encoder settlement\n\nCo-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>\"\n```\n"
  },
  "timestamp": 1773591635964
}
