{
  "sessionId": "cli-20260315-155606-d364b214",
  "toolName": "task",
  "toolArgs": {
    "agent_type": "general-purpose",
    "description": "ABR system T-019",
    "mode": "background",
    "prompt": "\nYou are building StreamCaster (`com.port80.app`), an iOS RTMP streaming app at `/Users/alex/Code/rtmp-client/iphone/StreamCaster/`.\n\n**Your Task (T-019):** Implement the Adaptive Bitrate (ABR) system with quality ladder and policy.\n\n**IMPORTANT CODE STYLE:** Add beginner-friendly comments. Prefer simple code.\n\n**First**, read existing files:\n```bash\ncat /Users/alex/Code/rtmp-client/iphone/StreamCaster/Services/EncoderController.swift\ncat /Users/alex/Code/rtmp-client/iphone/StreamCaster/Camera/DeviceCapabilityQuery.swift\ncat /Users/alex/Code/rtmp-client/iphone/StreamCaster/Camera/AVDeviceCapabilityQuery.swift\ncat /Users/alex/Code/rtmp-client/iphone/StreamCaster/Data/Models/StreamConfig.swift\ncat /Users/alex/Code/rtmp-client/iphone/StreamCaster/Data/Models/StreamStats.swift\n```\n\n**Requirements:**\n\n### 1. Create `Services/AbrLadder.swift`\n\n```swift\nimport Foundation\n\n/// AbrLadder defines the quality steps that the ABR system can use.\n///\n/// WHAT IS AN ABR LADDER?\n/// Think of it as a staircase of quality levels. When network conditions\n/// are good, we use a high step (great quality). When the network gets\n/// slow, we step down to lower quality to avoid buffering/stuttering.\n///\n/// The ladder is device-specific — older devices have fewer steps\n/// because they can't handle the higher quality levels.\n///\n/// STEP-DOWN ORDER (prefer least-disruptive changes first):\n/// 1. First, reduce BITRATE only (no encoder restart needed — instant!)\n/// 2. If bitrate reduction isn't enough, reduce RESOLUTION\n/// 3. As last resort, reduce FRAME RATE\nstruct AbrLadder {\n    \n    /// A single quality step in the ladder\n    struct Step: Equatable {\n        let resolution: Resolution\n        let fps: Int\n        let bitrateKbps: Int\n        \n        var description: String {\n            \"\\(resolution.description)@\\(fps)fps \\(bitrateKbps)kbps\"\n        }\n    }\n    \n    /// All available steps, ordered from highest to lowest quality\n    let steps: [Step]\n    \n    /// The current step index\n    private(set) var currentIndex: Int = 0\n    \n    /// Create a ladder based on the starting config and device capabilities\n    static func buildLadder(startingConfig: StreamConfig, deviceTier: Int) -> AbrLadder {\n        // Build steps based on starting resolution:\n        // 1080p30 → 720p30 → 540p30 → 480p30\n        // For each resolution: 100%, 75%, 50% bitrate sub-steps\n        // Frame rate steps: 30 → 24 → 15\n        // On Tier 1 devices: fewer steps, lower max\n    }\n    \n    /// Get the current step\n    var currentStep: Step { steps[currentIndex] }\n    \n    /// Can we step down further?\n    var canStepDown: Bool { currentIndex < steps.count - 1 }\n    \n    /// Can we step up (recover quality)?\n    var canStepUp: Bool { currentIndex > 0 }\n    \n    /// Step down to next lower quality\n    mutating func stepDown() -> Step? { ... }\n    \n    /// Step up to next higher quality\n    mutating func stepUp() -> Step? { ... }\n    \n    /// Reset to the top of the ladder\n    mutating func reset() { currentIndex = 0 }\n}\n```\n\n### 2. Create `Services/AbrPolicy.swift`\n\n```swift\nimport Foundation\nimport Combine\n\n/// AbrPolicy decides WHEN to change quality based on network conditions.\n///\n/// HOW IT WORKS:\n/// 1. Monitors the actual streaming bitrate vs. the configured bitrate\n/// 2. If actual bitrate is significantly below target for several seconds,\n///    network is congested → step down the quality ladder\n/// 3. If actual bitrate sustains at target for a longer period,\n///    network has improved → try stepping up the quality ladder\n/// 4. Also watches for encoder backpressure (fps drops below 80% of target\n///    for 5+ consecutive seconds) → this means the device can't keep up\n///\n/// The policy makes DECISIONS. The EncoderController EXECUTES them.\nfinal class AbrPolicy: ObservableObject {\n    \n    /// Whether ABR is currently enabled\n    @Published var isEnabled: Bool\n    \n    /// The quality ladder for this session\n    private var ladder: AbrLadder\n    \n    /// Reference to the encoder controller that executes changes\n    private let encoderController: EncoderController\n    \n    /// Backpressure detection: track consecutive low-fps seconds\n    private var consecutiveLowFpsSeconds: Int = 0\n    private let backpressureThreshold: Float = 0.8  // 80% of target fps\n    private let backpressureDurationSeconds: Int = 5\n    \n    /// Congestion detection\n    private var consecutiveLowBitrateSeconds: Int = 0\n    private let congestionDurationSeconds: Int = 3\n    \n    /// Recovery tracking\n    private var consecutiveGoodSeconds: Int = 0\n    private let recoveryDurationSeconds: Int = 10\n    \n    init(encoderController: EncoderController, startingConfig: StreamConfig, deviceTier: Int) { ... }\n    \n    /// Called every 1 second with new stats from the encoder\n    func evaluateStats(_ stats: StreamStats, targetFps: Int, targetBitrateKbps: Int) async {\n        guard isEnabled else { return }\n        \n        // 1. Check for encoder backpressure\n        // 2. Check for network congestion\n        // 3. Check for recovery opportunity\n    }\n    \n    /// Force step down (called by thermal system too)\n    func forceStepDown() async { ... }\n    \n    /// Reset the ladder (new session)\n    func reset(config: StreamConfig, deviceTier: Int) { ... }\n}\n```\n\nWire both into DependencyContainer.\n\nAfter completing, update SQL:\n```sql\nUPDATE todos SET status = 'done' WHERE id = 'T-019';\n```\n\nThen commit:\n```bash\ncd /Users/alex/Code/rtmp-client/iphone\ngit add -A\ngit commit -m \"feat: implement adaptive bitrate system with quality ladder\n\n- Add AbrLadder with device-tier-aware quality steps\n- Step-down order: bitrate first, then resolution, then fps\n- Add AbrPolicy for congestion detection and recovery\n- Backpressure detection: fps < 80% for 5s triggers step-down\n- Congestion detection: actual bitrate below target for 3s\n- Recovery: sustained good performance for 10s triggers step-up\n- Integrates with EncoderController for serialized changes\n- Wire into DependencyContainer\n\nCo-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>\"\n```\n"
  },
  "timestamp": 1773592052832
}
