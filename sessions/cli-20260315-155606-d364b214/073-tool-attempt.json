{
  "sessionId": "cli-20260315-155606-d364b214",
  "toolName": "task",
  "toolArgs": {
    "agent_type": "general-purpose",
    "description": "StreamViewModel T-010",
    "mode": "background",
    "prompt": "\nYou are building StreamCaster (`com.port80.app`), an iOS RTMP streaming app at `/Users/alex/Code/rtmp-client/iphone/StreamCaster/`.\n\n**Your Task (T-010):** Implement StreamViewModel that observes the StreamingEngine and projects state for SwiftUI.\n\n**IMPORTANT CODE STYLE:** Add beginner-friendly comments. Prefer simple code.\n\n**First**, read existing files:\n```bash\ncat /Users/alex/Code/rtmp-client/iphone/StreamCaster/Services/StreamingEngine.swift\ncat /Users/alex/Code/rtmp-client/iphone/StreamCaster/Services/StreamingEngineProtocol.swift\ncat /Users/alex/Code/rtmp-client/iphone/StreamCaster/Data/Models/StreamState.swift\ncat /Users/alex/Code/rtmp-client/iphone/StreamCaster/Data/Models/StreamStats.swift\n```\n\n**Requirements:**\n\nCreate `/Users/alex/Code/rtmp-client/iphone/StreamCaster/ViewModels/StreamViewModel.swift`:\n\n```swift\nimport Foundation\nimport Combine\nimport AVFoundation\n\n/// StreamViewModel is the bridge between the StreamingEngine and SwiftUI views.\n///\n/// It observes the engine's @Published properties and projects them into\n/// simple, UI-friendly values that SwiftUI views can easily display.\n///\n/// For example, instead of checking `snapshot.transport` for the exact state,\n/// the view can just check `isStreaming` or `statusText`.\n///\n/// ARCHITECTURE NOTE:\n/// - The ViewModel NEVER modifies engine state directly\n/// - It calls engine methods (startStream, stopStream, etc.)\n/// - It reads state from engine's @Published properties\n/// - All Combine subscriptions are stored in `cancellables` and cancelled in deinit\n@MainActor\nfinal class StreamViewModel: ObservableObject {\n    \n    // MARK: - Published Properties for UI\n    \n    /// The full session snapshot (for views that need detailed state)\n    @Published private(set) var sessionSnapshot: StreamSessionSnapshot = .idle\n    \n    /// Live stream statistics\n    @Published private(set) var streamStats: StreamStats = StreamStats()\n    \n    // Convenience properties for simpler UI binding\n    @Published private(set) var isStreaming: Bool = false\n    @Published private(set) var isConnecting: Bool = false\n    @Published private(set) var isReconnecting: Bool = false\n    @Published private(set) var isMuted: Bool = false\n    @Published private(set) var statusText: String = \"Ready\"\n    @Published private(set) var statusColor: String = \"gray\"  // For UI color coding\n    @Published private(set) var formattedDuration: String = \"00:00:00\"\n    @Published private(set) var formattedBitrate: String = \"0 kbps\"\n    @Published private(set) var canStartStream: Bool = true\n    @Published private(set) var canStopStream: Bool = false\n    @Published private(set) var showThermalWarning: Bool = false\n    \n    // MARK: - Dependencies\n    \n    private let engine: StreamingEngine\n    private var cancellables = Set<AnyCancellable>()\n    \n    init(engine: StreamingEngine = .shared) {\n        self.engine = engine\n        setupBindings()\n    }\n    \n    // MARK: - Setup\n    \n    private func setupBindings() {\n        // Subscribe to engine.$sessionSnapshot\n        // Map to convenience properties\n        // Subscribe to engine.$streamStats\n        // Format duration and bitrate strings\n    }\n    \n    // MARK: - Actions\n    \n    func startStream(profileId: String) { Task { try await engine.startStream(profileId: profileId) } }\n    func stopStream() { Task { await engine.stopStream(reason: .userRequest) } }\n    func toggleMute() { Task { await engine.toggleMute() } }\n    func switchCamera() { Task { try await engine.switchCamera() } }\n    \n    // MARK: - Formatting Helpers\n    \n    private func formatDuration(ms: Int64) -> String {\n        // Convert milliseconds to HH:MM:SS\n    }\n    \n    private func formatBitrate(kbps: Int) -> String {\n        // Format as \"2.5 Mbps\" or \"500 kbps\"\n    }\n    \n    private func statusText(for transport: TransportState) -> String {\n        // Return human-readable status\n    }\n}\n```\n\nKey requirements:\n1. Use `Combine` to observe `engine.$sessionSnapshot` and `engine.$streamStats`\n2. Map complex state to simple booleans and strings for UI\n3. All subscriptions in `cancellables` Set, cleared in deinit\n4. Format duration as HH:MM:SS\n5. Format bitrate as \"X.X Mbps\" or \"X kbps\"\n6. Status text: \"Ready\", \"Connecting...\", \"Live\", \"Reconnecting (attempt X)...\", \"Stopped\"\n7. Thermal warning when thermalLevel is .serious or .critical\n\nAfter completing, update SQL:\n```sql\nUPDATE todos SET status = 'done' WHERE id = 'T-010';\n```\n\nThen commit:\n```bash\ncd /Users/alex/Code/rtmp-client/iphone\ngit add -A\ngit commit -m \"feat: implement StreamViewModel bridging engine to SwiftUI\n\n- Observe StreamingEngine @Published properties via Combine\n- Project complex state into simple UI-friendly booleans and strings\n- Format duration (HH:MM:SS) and bitrate (X.X Mbps) for display\n- Provide convenience properties: isStreaming, statusText, canStart, etc.\n- All subscriptions managed in cancellables Set\n- Actions delegate to engine (never modify state directly)\n\nCo-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>\"\n```\n"
  },
  "timestamp": 1773591635840
}
