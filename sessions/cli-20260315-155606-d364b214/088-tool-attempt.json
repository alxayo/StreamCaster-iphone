{
  "sessionId": "cli-20260315-155606-d364b214",
  "toolName": "task",
  "toolArgs": {
    "agent_type": "general-purpose",
    "description": "Transport security T-018",
    "mode": "background",
    "prompt": "\nYou are building StreamCaster (`com.port80.app`), an iOS RTMP streaming app at `/Users/alex/Code/rtmp-client/iphone/StreamCaster/`.\n\n**Your Task (T-018):** Implement RTMPS transport security enforcement and TransportSecurityAlert view.\n\n**IMPORTANT CODE STYLE:** Add beginner-friendly comments. Prefer simple code.\n\n**First**, read existing files:\n```bash\ncat /Users/alex/Code/rtmp-client/iphone/StreamCaster/Services/ConnectionManager.swift\ncat /Users/alex/Code/rtmp-client/iphone/StreamCaster/Data/Models/EndpointProfile.swift\n```\n\n**Requirements:**\n\n### 1. Create `Services/TransportSecurityValidator.swift`\n\n```swift\nimport Foundation\n\n/// TransportSecurityValidator enforces the app's transport security rules.\n///\n/// SECURITY RULES:\n/// 1. RTMPS (rtmps://) — Always allowed. Uses system TLS. No custom cert bypass.\n/// 2. RTMP (rtmp://) with credentials — BLOCKED. Stream keys and passwords\n///    must NEVER be sent in plaintext. This is a hard security requirement.\n/// 3. RTMP (rtmp://) without credentials — Allowed. Some test/local servers\n///    don't need authentication, and plaintext is acceptable for those.\n///\n/// We use iOS's built-in TLS (Network.framework / URLSession). We NEVER:\n/// - Override SecTrustEvaluate\n/// - Set NSAllowsArbitraryLoads\n/// - Implement custom certificate pinning\n/// Users with self-signed certs must install them via iOS Settings.\nstruct TransportSecurityValidator {\n    \n    /// Result of a security validation check\n    enum ValidationResult {\n        case allowed                           // Safe to proceed\n        case blockedPlaintextWithCredentials    // rtmp:// with secrets — BLOCKED\n        case warningPlaintext                   // rtmp:// without secrets — warn but allow\n    }\n    \n    /// Validate whether a connection to this profile is allowed\n    static func validate(profile: EndpointProfile) -> ValidationResult {\n        let isSecure = profile.rtmpUrl.lowercased().hasPrefix(\"rtmps://\")\n        let hasCredentials = !profile.streamKey.isEmpty || \n                            profile.username != nil || \n                            profile.password != nil\n        \n        if isSecure { return .allowed }\n        if hasCredentials { return .blockedPlaintextWithCredentials }\n        return .warningPlaintext\n    }\n    \n    /// Check if a URL uses RTMPS\n    static func isSecureURL(_ url: String) -> Bool {\n        url.lowercased().hasPrefix(\"rtmps://\")\n    }\n}\n```\n\n### 2. Create `Views/Components/TransportSecurityAlert.swift`\n\n```swift\nimport SwiftUI\n\n/// TransportSecurityAlert shows security warnings about RTMP connections.\n///\n/// When a user tries to stream with credentials over plain rtmp://,\n/// this alert blocks them and explains why RTMPS is required.\nstruct TransportSecurityAlert: View {\n    let validationResult: TransportSecurityValidator.ValidationResult\n    let onDismiss: () -> Void\n    let onProceedAnyway: (() -> Void)?  // Only for warningPlaintext\n    \n    var body: some View {\n        // For .blockedPlaintextWithCredentials:\n        //   Red alert icon, \"Security Error\" title\n        //   \"Your stream key and credentials would be sent in plaintext...\"\n        //   \"Change your URL to rtmps:// to continue\"\n        //   Only a \"Dismiss\" button (no proceed option)\n        \n        // For .warningPlaintext:\n        //   Yellow warning icon, \"Security Warning\" title  \n        //   \"This connection is not encrypted...\"\n        //   \"Proceed Anyway\" and \"Cancel\" buttons\n    }\n}\n```\n\n### 3. Integrate into ConnectionManager\n\nRead and update ConnectionManager to call `TransportSecurityValidator.validate()` before connecting. If blocked, emit a connection event with `.errorAuth` reason.\n\nAfter completing, update SQL:\n```sql\nUPDATE todos SET status = 'done' WHERE id = 'T-018';\n```\n\nThen commit:\n```bash\ncd /Users/alex/Code/rtmp-client/iphone\ngit add -A\ngit commit -m \"feat: enforce RTMPS transport security for credentials\n\n- Add TransportSecurityValidator with three-tier validation:\n  RTMPS always allowed, RTMP+credentials blocked, RTMP-only warned\n- Add TransportSecurityAlert SwiftUI view for user-facing warnings\n- Integrate validation into ConnectionManager before connect\n- Hard-reject plaintext RTMP when stream key or auth is present\n- No custom SecTrustEvaluate or NSAllowsArbitraryLoads\n- System TLS only — self-signed certs via iOS Settings\n\nCo-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>\"\n```\n"
  },
  "timestamp": 1773592052709
}
